# Experiment execution
openai>=1.12.0

# Analysis
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
networkx>=3.0
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12

# Optional: semantic metrics (drift, mirroring)
# sentence-transformers>=2.2

# vLLM inference server (install separately on GPU nodes)
# vllm>=0.6.0

# ---------------------------------------------------------------------------
# A full 184-package snapshot of the dsail3 working environment is recorded in
# requirements-lock.txt (machine-generated, 2026-08-17). That file is evidence
# of the exact versions behind the results — it is NOT the file to install
# from. This one is: the omissions above (vllm, sentence-transformers) are
# deliberate, since those are installed separately on GPU nodes.
# ---------------------------------------------------------------------------
